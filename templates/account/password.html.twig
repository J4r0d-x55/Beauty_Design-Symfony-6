{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe{% endblock %}

{% block body %}
<div class="bloc-compte-welcome">
    <h1 class="title-compte">Modifier mon mot de passe</h1>
    <a class="retour-mon-compte" href="{{ path('app_account') }}">Retourner à mon compte</a><br/>
    <hr>
</div>
{% if notification %}
    <div class="alert alert-info">{{ notification }}</div>
{% endif %}

<div class="container-sm" style="margin-top: 50px;">
    {{ form(form) }}
</div>


{% endblock %}